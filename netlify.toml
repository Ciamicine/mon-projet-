[build]
  command = "echo 'Static HTML deployment' && mkdir -p dist && cp index-premium.html dist/index.html"
  publish = "dist"

[dev]
  command = "npm run dev"
  port = 5173

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
